#!/usr/bin/env bash

# Shared tool versions for platform setup scripts.
# Each value can still be overridden from the environment before running a script.
NEOVIM_VERSION="${NEOVIM_VERSION:-0.12.1}"
NODE_VERSION="${NODE_VERSION:-24}"
NVM_VERSION="${NVM_VERSION:-0.40.3}"
TREE_SITTER_CLI_VERSION="${TREE_SITTER_CLI_VERSION:-0.26.8}"
LAZYGIT_VERSION="${LAZYGIT_VERSION:-0.61.1}"
HACKGEN_VERSION="${HACKGEN_VERSION:-2.10.0}"
